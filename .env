
# Database configuration for XAMPP MySQL
DB_HOST=127.0.0.1
DB_USER=root
DB_PASS=
DB_NAME=sisoy_booking
DB_PORT=3306

# API URL - will be set dynamically based on available port
# Default fallback if script doesn't run
NEXT_PUBLIC_API_URL=http://localhost:3001/api
